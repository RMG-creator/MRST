classdef PolymerFlowPropertyFunctions < FlowPropertyFunctions

    methods
        function props = PolymerFlowPropertyFunctions(model)
            props = props@FlowPropertyFunctions();
            props.RelativePermeability = PolymerRelativePermeability(model);
            props.Mobility = PolymerMobility(model);
        end
    end
end